{
  "name": "kanban",
  "version": "0.2.0",
  "private": true,
  "description": "Repository wrapper for next-ui — a local web workspace for running coding agents. The product lives in next-ui/.",
  "homepage": "https://github.com/cline/kanban",
  "bugs": {
    "url": "https://github.com/cline/kanban/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cline/kanban"
  },
  "author": "Cline",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix next-ui install",
    "dev": "npm --prefix next-ui run dev",
    "build": "npm --prefix next-ui run build",
    "start": "npm --prefix next-ui start",
    "serve": "npm --prefix next-ui run serve",
    "typecheck": "npm --prefix next-ui run typecheck",
    "lint": "npm --prefix next-ui run lint",
    "test": "npm --prefix next-ui run test",
    "test:e2e": "npm --prefix next-ui run test:e2e"
  }
}
